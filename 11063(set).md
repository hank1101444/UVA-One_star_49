![](https://github.com/hank1101444/photo/blob/main/1.png)
## 這題可假設他都是排好的
## 注意題目可以讓自己相加
```cpp
#include <iostream>
using namespace std;
#include <vector>
#include <set>
int main()
{
	int n;
	vector <int> v;
	int flag = 1;
	while (cin >> n)
	{
		int a;
		for (int i = 0; i < n; i++)
		{
			cin >> a;
			v.push_back(a);

		}
		//cin.ignore();
		//char t;
		//cin.get(t);
		int f = 1;
		set <int> s;
	//	set <int> iterator:: it;
		for (int i = 0; i < v.size() - 1; i++)
		{
			for(int j=i;j <v.size();j++)
			{
			
				int temp = v[i] + v[j];
				if(!s.count(temp))
				{
					s.insert(temp);
				}
				else
				{
					cout << "Case #" << flag << ": It is not a B2-Sequence.\n" << endl;
					flag++;
					f=0;
					break;
				}
			
			}

			
			if (f == 0)
			{
				break;
			}
		}
		if (f == 1) {
			cout << "Case #" << flag << ": It is a B2-Sequence.\n" << endl;
			flag++;
		}
		
		v.clear();
	}

	return 0;
}
```
