# cmath 的角度是弧度 所以度數要先轉弧度
# ang (度) *= M_PI / 180 (度轉弧度)
```cpp
#define _USE_MATH_DEFINES
#include <iostream>
using namespace std;
#include <cmath>
#include <string>
#include <cstdio>
int main()
{
	string s;
	int dis;
	double ang;
	while (cin >> dis >> ang >> s)
	{
		dis += 6440;
		if (s == "min")
			ang /= 60;
		// arc
		double arc = double(2 * M_PI * dis *ang/360);
		ang *= M_PI/180;
		double chord = sqrt(2*pow(dis,2) - 2*pow(dis,2)*cos(ang));
		printf("%.6lf %.6lf\n", arc, chord);
	}
	return 0;
}
```
