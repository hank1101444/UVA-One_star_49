```cpp
#include <iostream>
using namespace std;
#include <vector>
#include <algorithm>
#include <cmath>
struct node {
	int num;
	//int mod = n % divisor;
	int mod;
	//int mod_2 = n % 2;
	int mod_2;
};

bool cmp(node t1, node t2) {
	if (t1.mod_2 == 1 && t2.mod_2 == 1)//皆為奇 大奇前 小奇後
		return t1.num > t2.num;
	if (t1.mod_2 == 0 && t2.mod_2 == 0)//皆為偶 小偶前 大偶後
		return t1.num < t2.num;
	if (t1.mod == t2.mod)//餘數同 奇數前 偶數後
		return t1.mod_2 == 1;

	return t1.mod < t2.mod;//餘數 小者前 大者後
}

int main() {
	vector <node>v;
	int n;
	int divisor = 0;
	while (cin >> n >> divisor) {
		cout << n << ' ' << divisor << endl;
		if (n == 0 && divisor == 0)
			break;
		while (n--) {
			node tmp;
			cin >> tmp.num;
			tmp.mod = tmp.num % divisor;
			tmp.mod_2 = abs(tmp.num % 2);
			v.push_back(tmp);
		}
		sort(v.begin(), v.end(), cmp);
		for (int i = 0; i < v.size(); i++) {
			cout << v[i].num << endl;
		}
		v.clear();
	}
	return 0;
}
```
