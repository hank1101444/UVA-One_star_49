## 後面的字串要=目前最大位 ex: 4, 2->4,6,10,1->10 輸出時最大位一下的都不會再輸出了

```cpp
#include <iostream>
using namespace std;
#include <cstring>
#include <string>
int main() {
	int len[100] = { 0 };
	char a[100][101] = { '\0' };
	int n = 0, max = 0, tmp = 0, cur = 0;
	while (cin.getline(a[n], 200)) {
		len[n] = strlen(a[n]);
		if (len[n] > max)
			max = len[n];
		else if (max > len[n]) {
			for (int i = len[n]; i < max; ++i) {
				a[n][i] = ' ';
				++len[n];
			}	
		}
		++n;
	}
	for (int i = 0; i < max; ++i) {
		for (int j = n - 1; j >= 0; --j) {
			if(i<len[j])
				cout << a[j][i];
		}
		cout << endl;
	}
	return 0;
}
```
