```cpp
#include <iostream>
using namespace std;
#include <stack>
int main()
{
	int n;
	while(cin >> n)
	{
		if(!n)
		{
			break;
		}
		int temp;
		stack <int> s;
		while(n!=0)
		{
			temp = n%2;
			s.push(temp);
			n/=2;
		}
		cout << "The parity of ";
		int ans = 0;
		while(!s.empty())
		{
			ans+= s.top();
			cout << s.top();
			s.pop();
		
		}
		cout << " is " << ans << " (mod 2)." << endl;
	
	}

	return 0;
}
```
