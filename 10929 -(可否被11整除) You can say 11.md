# 判斷可否被11整除 看奇數位相加 減掉 偶數位相加 再除11
# 注意測茲有 00123所以要用string
&nbsp;
## 要改string
```cpp
// wrong
long long int tmp = n;
		bool flag = true;
		while (tmp) {
			if (flag) {
				odd += tmp % 10;
				flag = false;
			}
			else {
				even += tmp % 10;
				flag = true;
			}
			tmp /= 10;
		}
```
&nbsp;

&nbsp;
```cpp
#include <iostream>
using namespace std;
#include <cmath>
#include <string>
int main() {
	string s;
	while (cin >> s) {
		if (s == "0")
			break;
		int a[10000] = {0};
		long long int odd = 0;
		long long int even = 0;
		for (int i = 0; i < s.size(); i++) {
			a[i] = s[i] - '0';
			//奇數位
			if (i % 2 == 0)
				odd += a[i];
			else 
				even += a[i];
		}
		if (abs(odd - even) % 11 == 0) {
			cout << s << " is a multiple of 11." << endl;
		}
		else {
			cout << s << " is not a multiple of 11." << endl;
		}
	}
	return 0;
}
```
