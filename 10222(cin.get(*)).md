## char 解法
## strlen() 在csring
```cpp
#include <iostream>
using namespace std;
#include <cstring>
int main() {
	char s[] = "`1234567890-=qwertyuiop[]\\asdfghjkl;'zxcvbnm,./";
	char c;
	while (cin.get(c)) {
		if (c == '\n')
			break;
		else if (c == ' ') {
			cout << ' ';
		}
		else if ('A' <= c && c <= 'Z')
			c = c - 'A' + 'a';
		for (int i = 0; i < strlen(s); ++i) {
			if (c == s[i])
				cout << s[i - 2];
		}
		
	}
	cout << endl;
	return 0;
}
```

## string find()
```cpp
#include <iostream>
using namespace std;
#include <string>
int main() {
	string s2 = "`1234567890-=qwertyuiop[]\\asdfghjkl;'zxcvbnm,./";
	string s;
	getline(cin, s);
	
	for (int i = 0; i < s.size(); ++i) {
		if (s[i] == ' ') 
			cout << ' ';
		if ('A' <= s[i] && s[i] <= 'Z')
			s[i] = s[i] - 'A' + 'a';
		size_t tmp = s2.find(s[i]);
		if (tmp != string::npos) 
			cout << s2[tmp - 2];
		
	}
	cout << endl;
	return 0;
}
```
