

![image](https://github.com/hank1101444/photo/blob/main/10222.png)
```cpp
#include <iostream>
using namespace std;
#include <cctype>.
// uva 會compilation errow
# include <cstring>
int main() {

	char a[] = "`1234567890-=qwertyuiop[]\\"
		"asdfghjkl;'"
		"zxcvbnm,./";
	int num = strlen(a);

	char b;
	char* c;
	while (cin.get(b)) {
		b = tolower(b);
		int flag = 0;
		for (int i = 0; i < num; i++) {
			if (b == a[i]) {
				c = &a[i];
				cout << *(c - 2);
				break;
			}
			flag++;
		}
		if(flag == num) {
			cout << b;
		}
		//讀字元不用 ignore
		//cin.ignore();
	}


	return 0;
}
```
