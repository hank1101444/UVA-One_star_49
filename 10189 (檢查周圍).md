# 記得 如果是4*4 那終止條件要 <4 等同於 >3 時 break; 

```cpp
#include <iostream>
using namespace std;
#include <string>
#include <vector>


int count_t(vector <string> v, int y, int x)
{
	char flag = '*';
	int ans = 0;
	for (int i = y - 1; i <= y + 1; i++)
	{
		if (i < 0)
			continue;
		/////////////////////////////////////
		// 要記得 -1
		else if (i > v.size() - 1)
			break;
		for (int j = x - 1; j <= x + 1; j++)
		{
			if (j < 0)
				continue;
			else if (j > v[i].size() - 1)
				break;
			if (v[i][j] == '*')
				ans++;
		}
	}
	return ans;
}

void check(vector <string>& v)
{
	for (int i = 0; i < v.size(); i++)
	{
		for (int j = 0; j < v[i].size(); j++)
		{
			if (v[i][j] == '*')
				continue;
			int ans;
			ans = count_t(v, i, j);
			v[i][j] = ans + '0';
		}
	}
}

int main()
{
	vector <string> v;
	string s;
	int y, x;
	int flag = 1;
	bool end_check = false;
	while (cin >> y >> x)
	{
		for (int i = 0; i < y; i++)
		{
			cin >> s;
			v.push_back(s);
		}
		check(v);
		if (!end_check)
			end_check = true;
		else
			cout << endl;
		cout << "Field #" << flag << ":\n";
		flag++;
		for (int i = 0; i < v.size(); i++)
			cout << v[i] << endl;
		v.clear();
	}

	return 0;
}
```
