# 倍數計算用for
```cpp
#include <iostream>
using namespace std;

int main()
{
	int n;
	cin >> n;
	while (n--)
	{
		int ans = 0;
		int day, pol;
		int data[3651] = {};
		cin >> day >> pol;
		for (int i = 0; i < pol; i++)
		{
			int parameter;
			int tmp_par;
			cin >> parameter;
			//tmp_par = parameter;  // delete
			//int j = 1;			// delete
			//while (parameter <= day)  // delete
			//{
			//	data[parameter] = 1;
			//	parameter = tmp_par * j;
			//	j++;
			//}
			// 倍數改寫
			for (int i = parameter; i <= day; i += parameter) {
				data[i] = 1;
			}
		}
		//int last_day = 6;
		//int i = 1;
		/*while (last_day <= day)
		{
			data[last_day] = 0;
			data[last_day + 1] = 0;
			last_day += 7 * i;
		}*/
		for (int i = 6; i <= day; i += 7) {
			data[i] = 0;
			data[i + 1] = 0;
		}

		for (int i = 0; i <= day; i++)
		{
			ans += data[i];
		}

		cout << ans << endl;

	}


	return 0;
}
```
