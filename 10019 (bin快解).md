```cpp
#include <iostream>
using namespace std;
#include <cmath>
int count_dec_to_bin(int a)
{
	//int flag = 0;
	//while (a != 0)
	//{
	//	int temp = a % 2;
	//	a /= 2;
	//	if (temp == 1)
	//	{
	//		flag++;
	//	}
	//}

	//改版
	int flag = 0;
	for (int v = a; v; v /= 2)
		flag += v % 2;
	return flag;

}

int main()
{

	int n;
	int ans = 0;
	cin >> n;
	while (n--)
	{
		int num;
		cin >> num;
		ans = count_dec_to_bin(num);
		cout << ans << ' ';
		int dec = 0, i = 0;
		while (num != 0)
		{
			dec += (num % 10) * pow(16, i);
			i++;
			num /= 10;
		}
		ans = count_dec_to_bin(dec);
		cout << ans << endl;
	}
	return 0;
}
```
