![](https://github.com/hank1101444/photo/blob/main/1.png)
## N= 是使用者輸入的
```cpp
#include <iostream>
using namespace std;

int main()
{
	int n, test = 1;
	cin >> n;
	while (n--)
	{
		int dim, flag = 1;
		int m[100][100] = {};
		char a;
		cin >> a;
		cin >> a;
		cin >> dim;
		for (int i = 0; i < dim; i++)
		{
			for (int j = 0; j < dim; j++)
			{
				int temp;
				cin >> temp;
				m[i][j] = temp;
			}
		}

		for (int i = 0; i < dim; i++)
		{
			for (int j = 0; j < dim; j++)
			{
				if (m[i][j] < 0)
				{
					flag = 0;
				}
				if (m[i][j] != m[dim - i - 1][dim - j - 1])
				{
					flag = 0;
				}
			}
		}
		if (flag == 0)
		{
			cout << "Test #" << test << ": Non-symmetric." << endl;
		}
		else
		{
			cout << "Test #" << test << ": Symmetric." << endl;
		}

		test++;
	}
	return 0;
}
```
