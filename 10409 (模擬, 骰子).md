```cpp
#include <iostream>
using namespace std;
#include <string>

int die[6] = {1,6,4,3,5,2}; // u,d,e,w,s,n

void sd ()
{
	// u,d,e,w,s,n
	int tmp = die[1];
	die[1] = die[4];//d
	die[4] = die[0];//s
	die[0] = die[5];//u
    die[5] = tmp;//n
}

void ed ()
{
	// u,d,e,w,s,n
	int tmp = die[2];
	die[2] = die[0];//e
	die[0] = die[3];//u 
	die[3] = die[1];//w
    die[1] = tmp;//d
}

void wd ()
{
	// u,d,e,w,s,n
	int tmp = die[3];
	die[3] = die[0];//w
	die[0] = die[2];//u
	die[2] = die[1];//e 
    die[1] = tmp;//d
}

void nd ()
{
	// u,d,e,w,s,n
	int tmp = die[0];
	die[0] = die[4];//u
	die[4] = die[1];//s
	die[1] = die[5];//d
    die[5] = tmp;//n
}

int main(){
	int n;
	while(cin >> n)
	{
		if(n == 0)
			break;
		string s;
		//die[6] = {1,6,4,3,5,2}; // u,d,e,w,s,n
		die[0] = 1;
		die[1] = 6;
		die[2] = 4;
		die[3] = 3;
		die[4] = 5;
		die[5] = 2;
		 
		for(int i = 0;i <n;i++)
		{
			cin >> s;
			if(s == "east")
			{
				ed();
			}
			if(s == "west")
			{
				wd();
			}
			if(s == "south")
			{
				sd();
			}
			if(s == "north")
			{
				nd();
			}
			
		}
		cout << die[0] << endl;
	
	}
	return 0;
}
```
