# 看清楚題目限制格式 注意+s


```
# include<iostream>
using namespace std;
# include<cstring>


int main() {
	
	
	while(1){
		char A[101], B[101];
		
		cin >> A >> B;
		int a[101] = {}, b[101] = {};
		int astrlen = 0, bstrlen = 0;

		astrlen = strlen(A);
		bstrlen = strlen(B);

		if (astrlen == 1 && bstrlen == 1 && A[0] == '0' && B[0] == '0')
			break;
      
    
              //換int將最大位放到最後面
		for (int i = 0; i < astrlen; i++) {
			a[i] = A[astrlen - 1 - i] - '0';

		}

		for (int i = 0; i < bstrlen; i++) {
			b[i] = B[bstrlen - 1 - i] - '0';
		}

		//adition
		//answer = a+b
		//count = carry operation
		int count = 0, ans[101] = {};
		int ansSize = 0;

		if (astrlen > bstrlen)
			ansSize = astrlen+1;		// modify
		else
			ansSize = bstrlen+1;		// modify

		for (int i = 0; i < ansSize; i++) {
			ans[i] = b[i] + a[i];
		}

		for (int i = 0; i < ansSize; i++) {
			while (ans[i] >= 10) {
				ans[i + 1] += 1;
				ans[i] -= 10;
				count++;
			}
		}

		/*while (ans[ansSize - 1] == 0) {
			ansSize--;
		}*/

		if (count == 0) {
			cout << "No carry operation."<< endl;
		}
		else if(count == 1){			//add
			cout << count << " carry operation." << endl;
		}

		else {
			cout << count << " carry operations." << endl;
		}

	
	
	}

	return 0;
}
```
