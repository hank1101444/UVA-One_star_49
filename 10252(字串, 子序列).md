
# 注意題目說是要最長，給兩字串找出共同的最長字串
## 空字串也是字串所以要用getline
### 想法 : 用兩個ascii table 
```cpp
#include <string>
using namespace std;
#include <iostream>
#include <cstring>
int main() {
	char a[1001], b[1001];
	while (cin.getline(a, 1001) && cin.getline(b, 1001)) {
		int tableA[150] = { 0 }, tableB[150] = { 0 };
		//if (cin.eof())
		//	break;
		int a_size = strlen(a), b_size = strlen(b);
		if (a_size == b_size && a_size == 0) {
			cout << endl;
			continue;
		}
		for (int i = 0; i < a_size; ++i) {
			++tableA[int(a[i])];
		}
		for (int i = 0; i < b_size; ++i) {
			++tableB[int(b[i])];
		}
		for (int i = int('a'); i <= int('z'); ++i) {
			while (tableA[i] && tableB[i]) {
				cout << char(i);
				--tableA[i];
				--tableB[i];
			}
		}
		cout << endl;
	}
	return 0;
}
```
