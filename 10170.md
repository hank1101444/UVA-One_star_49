```cpp
#include <iostream>
using namespace std;
int main()
{
	int init_n;
	while(cin >> init_n)
	{
		long long day;
		cin >> day;
		while(day>0)
		{
			day -= init_n;
			if(day<=0)
			{
				break;
			}
			init_n++;
		}
		cout << init_n << endl;
	
	}
	return 0;
}
```
