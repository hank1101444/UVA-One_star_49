## 本題是由f(1) = 1, f(2) = 2 開始
```cpp
#include <iostream>
using namespace std;

int main(){
	
	// 先用電腦觀察 m <1 00 000 000
	// f[40] = 102334155 所以只要到f[39]
	// 建立fib
	int f[40] = {};
	f[0] = 0;
	f[1] = 1;
	for (int i = 2; i < 40; i++) {
		f[i] = f[i - 1] + f[i - 2];
	}
	int n;
	cin >> n;
	while(n--)
	{
		int a;
		cin >> a;
		cout << a <<" = ";
		//卡住
		// 題目要求不能發生相鄰1的狀況
		// 因為是使用費式數列 , 所以由大減到小不可能會有相鄰狀況發生
		int flag = 0;
		for (int i = 39; i >= 2; i--) {
			if (a - f[i] >= 0) {
				a -= f[i];
				cout << '1';
				flag = 1;
			}
			// 先出現一次 1 後才可輸出0
			else if(flag == 1){
				cout << '0';
			}
		}
		cout << " (fib)" << endl;
	}
	return 0;
}
```
