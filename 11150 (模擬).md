```cpp
#include <iostream>
using namespace std;

int main()
{
	while (1)
	{
		//不管有沒有喝完 算瓶子
		int current_coke;
		cin >> current_coke;
		if (cin.eof())
			break;
		int remain = 0, sum = current_coke;
		while (current_coke >= 3)
		{
			remain = current_coke % 3;
			//算出能兌換的
			current_coke /= 3;
			sum += current_coke;
			current_coke += remain;
		}
		if (current_coke == 2)
		{
			sum++;
		}
		cout << sum << endl;
	}

	return 0;
}
```
