## 題目沒有說集合一定要連續
## 卡在n = 1 時 ,  n = 1其實也符合jolly 因為 Dn = 0 
## 使用set 時機:
## 不要重複的數字,而且有排序
```cpp
#include <iostream>
using namespace std;
#include <set>
#include <string>
#include <cmath>
int main() {
	set <int> s;
	string str;
	int tp1, tp2, n;

	while (cin >> n) {
		s.clear();
		cin >> tp1;
	/*	if (n == 1) {
			cout << "Jolly\n";
			continue;
		}*/
		for (int i = 1; i < n; ++i) {
			cin >> tp2;
			int ins = abs(tp1 - tp2);
			if (ins >= 1 && ins < n)
				s.insert(ins);
			tp1 = tp2;
		}
		if (s.size() == n - 1)
			cout << "Jolly\n";
		else
			cout << "Not jolly\n";
	}

	return 0;
}
```


