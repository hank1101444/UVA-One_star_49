## 題目沒有說集合一定要連續
```cpp
#include <iostream>
using namespace std;
#include <string>
#include <vector>
#include <cmath>
#include <cstring>
#include <algorithm>
bool check(const vector <int> a, int n);
void differ(vector <int>& a);

int main() {
	vector <string> b;
	char temp[3000];
	vector <int> z;
	int n;
	while (cin >> n) {
		//cin.get(temp, 3000);
		//cin.ignore();
		for (int i = 0; i < n; i++) {
			cin >> temp;
			int a = atoi(temp);
			z.push_back(a);
		}

		differ(z);
		/*for (int i = 0; i < z.size(); i++) {
			cout << z[i];
		}*/
		sort(z.begin(), z.end());
		if (check(z, n) || n == 1)
			cout << "Jolly" << endl;
		else
			cout << "Not Jolly" << endl;
		// add
		z.clear();
	}
	return 0;
}

bool check(const vector <int> a, int n) {

	for (int i = 0; i < n - 1; i++) {
		if (a[i] != i + 1) {
			return false;
		}
	}
	return true;
}

void differ(vector <int>& a) {
	vector <int> temp;
	for (int i = 0; i < a.size() - 1; i++) {
		int difference = abs(a[i] - a[i + 1]);
		temp.push_back(difference);
	}
	if (temp.size() != 0) {
		a = temp;
	}
}
```
&nbsp
&nbsp
## 使用set 時機:
## 不要重複的數字,而且有排序
```cpp
#include <iostream>
using namespace std;
#include <set>
#include <cstdlib>
int main() {
	int n;
	while (cin >> n) {
		int a1,a2;
		int temp =0;
		set <int> b;
		// 要先讀一次不然if條件不能下
		cin >> a1;
		for (int i = 1; i < n; i++) { //modify
			cin >> a2;
			int dif = abs(a1-a2);
			if (dif < n && dif >= 1) {
				b.insert(dif);
			}
			a1 = a2;
		}

		if (b.size() == n - 1)
			cout << "Jolly" << endl;
		else
			cout << "Not jolly" << endl;

		//set<int>::iterator i;
		//i = b.begin();
		//for (i = b.begin(); i != b.end(); i++) {
		//	cout << *i;
		//}

		b.clear();
	}



	return 0;
}
```
