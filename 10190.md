```cpp
#include <iostream>
using namespace std;
#include <vector>
int main()
{
	int a, b;
	vector <int> v;

	while (cin >> a >> b)
	{
		bool flag = false;
		int temp = a;
		// 讓temp 去變動不要更改到原始資料
		while (1)
		{
			if (a <= 1 || b <= 1)    // modify
			{
				break;
			}
			if (temp == 1)
			{
				v.push_back(temp);
				flag = true;
				break;
			}
			if (temp % b == 0)
			{
				v.push_back(temp);
				temp /= b;
			}
			else
			{
				break;
			}
		}
		if (flag)
		{
			for (int i = 0; i < v.size(); i++)
			{
				cout << v[i];
				if (i != v.size() - 1)
				{
					cout << ' ';
				}

			}
			cout << endl;
		}

		else
		{
			cout << "Boring!" << endl;
		}

		v.clear();

	}

	return 0;
}
```
