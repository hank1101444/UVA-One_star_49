
![image](https://github.com/hank1101444/photo/blob/main/10252.png)
# 注意題目說是要最長
## 空字串也是字串所以要用getline
```cpp
#include <iostream>
using namespace std;
#include <vector>
#include <string>
#include <algorithm>


struct z {
	char alp;
};
bool cmp(z a, z b);

int main() {

	string str1;
	string str2;
	vector<z> data;
	// 空字串也是字串!!
	// 不可用cin
	// cin >> str1 >>str2
	while (getline(cin, str1), getline(cin, str2)) {
		int flag = 0;
		for (int i = 0; i < str1.size(); i++) {
			
			for (int j = 0; j < str2.size(); j++) {
				if (str1[i] == str2[j]) {
					z temp;
					temp.alp = str1[i];
					data.push_back(temp);
					str1.erase(i, 1);
					str2.erase(j, 1);
					i--;
				}
			}
		}

		sort(data.begin(), data.end(), cmp);

		for (int i = 0; i < data.size(); i++) {
			cout << data[i].alp;
		}
		cout << endl;
		data.clear();
	}

	return 0;
}


bool cmp(z a, z b) {
	return a.alp < b.alp;
}
```
