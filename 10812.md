```cpp
#include <iostream>
using namespace std;

int main()
{
	int n;
	cin >> n;
	while(n--)
	{
		int sum, dif;
		cin >> sum >> dif;
		int x =0, y =0;
		if((sum+dif)%2 != 0 || (sum-dif)%2 !=0)
		{
			cout << "impossible" << endl;
		}
		else
		{
			x = (sum + dif)/2;
			y = (sum - dif)/2;
			if(x < 0 || y < 0)
			{
				cout << "impossible" << endl; 
			}
			else
			{
				if(x>y)
				{
					cout << x << ' ' << y << endl;
				}
				else
				{
					cout << y << ' ' << x << endl;
				}
			}
		}
	}
	return 0;
}
```
