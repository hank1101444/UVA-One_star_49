# 注意題目的x,y軸相反

```cpp
#include <iostream>
using namespace std;

int count(int x, int y)
{
	int ans = 0;
	int y_ans = double(y + 1) / 2 * (0 + y);
	int x_ans = double(x) / 2 * (2 * (y + 2) + (x - 1) * 1);
	ans += x_ans + y_ans;
	return ans;
}

int main()
{

	int x1, x2, y1, y2, flag = 1;
	int num;
	cin >> num;
	while (num--)
	{
		cin >> x1 >> y1 >> x2 >> y2;
		int ans = 0;
		// x= y , y = x
		ans = count(x2, y2) - count(x1, y1);
		cout << "Case " << flag << ": " <<ans << endl;
		flag++;
	}

	return 0;
}
```
