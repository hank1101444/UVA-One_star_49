#include <iostream>
using namespace std;
#include <string>
#include <algorithm>
#include <vector>

struct node {
	char a;
	int times = 0;
};

bool cmp(node n1, node n2) {
	if (n1.times == n2.times) {
		return n1.a < n2.a;
	}
	return n1.times > n2.times;
}

int main() {
	//int a[150] = { 0 };
	vector <node> v;
	string s;
	int n;
	cin >> n;
	cin.ignore();
	while (n--) {
		
		getline(cin, s);
		for (int i = 0; i < s.size(); i++) {
			if ('a' <= s[i] && s[i] <= 'z') 
				s[i] = s[i] - 'a' + 'A';
			else if ('A' <= s[i] && s[i] <= 'Z') {}
			else continue;
			int flag = 0;
			for (int j = 0; j < v.size(); ++j,++flag) {
				if (s[i] == v[j].a) {
					v[j].times++;
					break;
				}
			}
			if (flag == v.size()) {
				node tmp;
				tmp.a = s[i];
				tmp.times = 1;
				v.push_back(tmp);
			}
		}
	}

	sort(v.begin(), v.end(),cmp);
	for (int i = 0; i < v.size(); ++i) {
		cout << v[i].a << ' ' << v[i].times << endl;
	}
	return 0;

}
