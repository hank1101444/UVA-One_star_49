
```cpp
# include <iostream>
using namespace std;
# include <vector>
# include <string>
# include <algorithm>


struct z {
	char alp;
	int count = 1;
};
// 一定要寫在 struct z 後面 不然會出錯
bool cmp(z a, z b);


int main() {
	
	int num;
	vector <z> data;
	string str;


	cin >> num;
	cin.ignore();
	for (int i = 0; i < num; i++) {
		getline(cin, str);
		for (int j = 0; j < str.size(); j++) {
			if (str[j] <= 'z' && str[j] >= 'a') {
				str[j] = str[j] - 'a' + 'A';
			}
			int flag = 0;
			for (int k = 0; k < data.size(); k++) {
				if (data[k].alp == str[j]) {
					data[k].count++;
					break;
				}
				flag++;
			}
			if (flag == data.size()) {
				// 不能寫data.push_back(str[j]) 要多寫一個 struct object
				z temp;
				temp.alp = str[j];
				data.push_back(temp);
			}
		}
	}

	sort(data.begin(), data.end(), cmp);

	for (int i = 0; i < data.size(); i++) {
		if (data[i].alp >= 'A' && data[i].alp <= 'Z') {
			cout << data[i].alp << " " << data[i].count << endl;
		}
	}
	data.clear();
	return 0;
}


bool cmp(z a, z b){
	if (a.count == b.count) {
		if (a.alp < b.alp)
			return true;
		else
			return false;
	}
	else
		return a.count > b.count;
		
}
```
